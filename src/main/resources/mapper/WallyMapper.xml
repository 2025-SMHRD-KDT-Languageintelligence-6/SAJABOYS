<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.project.chaser.mapper.WallyMapper">

    <!-- 게임 데이터를 DB에 저장하는 SQL 쿼리 -->
    <insert id="insertGaming" parameterType="com.project.chaser.dto.Gaming">
        INSERT INTO gaming (user_idx, fes_idx, game_name, game_result, created_at)
        VALUES (#{userIdx}, #{fesIdx}, #{gameName}, #{gameResult}, #{createdAt});
    </insert>
</mapper>
